export const Search = () => {
    const handleChange = () => { };

    return (
        <div>
            <div>What would you like to search for?</div>
            <input type="text" placeholder="Search..." onChange={handleChange} />
        </div>
    );
};

